<section id="servicios" class="services-wrapper">
    <h2 class="services-title">Experiencias Didácticas</h2>
    <div class="carousel">
        <input type="radio" name="slider" id="s1" checked>
        <input type="radio" name="slider" id="s2">
        <input type="radio" name="slider" id="s3">
        
        <div class="cards">
          <label for="s1" class="card" id="card1">
 <div class="info">
<h3>Didáctica Interactiva</h3>
                <p>Convertimos el aprendizaje en un juego educativo.</p>

              </div>

              <div class="img-placeholder">IMG</div>
                                         </label>
            <label for="s2" class="card" id="card2">
                <div class="img-placeholder"></div>
                <h3>Gestión Escolar</h3>
                <p>Administración académica optimizada al máximo.</p>
            </label>
            <label for="s3" class="card" id="card3">
                <div class="img-placeholder"></div>
                <h3>Soporte 24/7</h3>
                <p>Acompañamiento digital constante para todos.</p>
            </label>
        </div>
    </div>
</section>

<style>
    .services-wrapper {
        padding: var(--spacing-2xl) 0;
        background: hsl(var(--background));
        overflow: hidden;
    }

    .services-title {
        text-align: center;
        font-size: 3rem;
        margin-bottom: var(--spacing-xl);
        color: hsl(var(--foreground));
    }

    .carousel {
        display: flex;
        justify-content: center;
        align-items: center;
        perspective: 1000px;
    }

    .cards {
        position: relative;
        width: 750px;
        height: 400px;
        transform-style: preserve-3d;
        transition: transform var(--transition-slow);
    }

    .card {
        position: absolute;
        width: 100%;
        height: 100%;
        color: hsl(var(--primary-foreground));
        background: hsl(var(--primary));
        border: 1px solid hsl(var(--border));
        border-radius: var(--radius);
        padding: var(--spacing-md);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--spacing-md);
        transition: transform var(--transition-slow), opacity var(--transition-slow);
        cursor: pointer;
        box-shadow: var(--shadow-lg);
    }

    .img-placeholder {
        width: 100%;
        height: 150px;        
        border-radius: var(--radius);
        color: hsl(var(--primary-foreground));
    }

    #card1 {
      display: flex;
      flex-direction: row;
      justify-content: space-evenly;
    }

    .info {
      border: 2px solid white;
      width: 100%;
      margin-right: 288px;
    }

    /* Carousel Logic */
    #s1:checked ~ .cards #card1,
    #s2:checked ~ .cards #card2,
    #s3:checked ~ .cards #card3 {
        transform: scale(1.1);
        z-index: 2;
        opacity: 1;
    }

    #s1:checked ~ .cards #card2,
    #s2:checked ~ .cards #card3,
    #s3:checked ~ .cards #card1 {
        transform: translateX(100%) scale(0.6);
        opacity: 0.6;
    }

    #s1:checked ~ .cards #card3,
    #s2:checked ~ .cards #card1,
    #s3:checked ~ .cards #card2 {
        transform: translateX(-100%) scale(0.5);
        opacity: 0.6;
    }

    input { display: none; }
</style>
