<main class="hero">
    <div class="hero-content">
        <div class="hero-presen">
            <h1>Bienvenido a Sistek</h1>
            <p>Lorem ipsum, dolor sit amet consectetur adipisicing elit. Repellat eaque id harum rerum provident! Dolor praesentium quo numquam eligendi repudiandae itaque. Voluptas excepturi sapiente ipsa blanditiis ducimus facilis cum ad.</p>
        </div>
        <div class="hero-img">
            <div class="img"></div>
        </div>
    </div>
</main>

<style is:global>

    .hero {        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 40px;
        gap: 40px;
        min-height: 100vh;
    }

    .hero-presen {
        max-width: 600px;
    }

    .hero-content {
        display: flex;
        flex-direction: row;
        align-items: center;
        justify-content: center;
        width: 100%;
        color: hsl(var(--foreground));
    }

    .hero-content h1 {
        font-size: 3.5rem;
        font-weight: 700;
        margin: 0 0 20px 0;
        line-height: 1.2;
    }

    .hero-content p {
        font-size: 1.3rem;
        margin: 0 0 30px 0;
        opacity: 0.95;
        font-weight: 300;
    }

    .btn {
        align-self: flex-start;
        padding: 14px 40px;
        color: hsl(var(--primary));
        border: none;
        border-radius: 50px;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .btn:hover {
        transform: translateY(-3px);
    }

    .hero-img {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .img {
        width: 100%;
        max-width: 400px;
        height: 300px;
        border-radius: 20px;
        background-color: hsl(var(--primary));
    }

    @media (max-width: 768px) {
        .hero {
            flex-direction: column;
            padding: 40px 20px;
            gap: 30px;
        }

        .hero-content h1 {
            font-size: 2.5rem;
        }

        .hero-content p {
            font-size: 1.1rem;
        }

        .img {
            max-width: 300px;
        }
    }

    @media (max-width: 480px) {
        .hero {
            padding: 30px 15px;
        }

        .hero-content h1 {
            font-size: 1.8rem;
        }

        .hero-content p {
            font-size: 1rem;
        }

        .btn {
            padding: 12px 30px;
            font-size: 1rem;
        }
    }
</style>