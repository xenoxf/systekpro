<section id="inicio" class="hero-container">
    <div class="hero-content">
        <h1 class="hero-title">Sistek Pro</h1>
        <p class="hero-description"> Lorem ipsum dolor sit amet consectetur adipisicing elit. Nemo omnis consectetur sit officiis architecto exercitationem dicta eum odio laboriosam! Sit </p>
        <div class="hero-actions">
            <button class="hero-button primary">Contactanos</button>        </div>
    </div>
    <div class="hero-visual">
        <div class="hero-blob"></div>
    </div>
</section>

<style>
    .hero-container {
        display: grid;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        gap: var(--spacing-2xl);
        padding: var(--spacing-2xl) var(--spacing-md);
        max-width: 1200px;
        margin: 0 auto;
        min-height: 80vh;
    }

    .hero-content {
        display: flex;
        flex-direction: column;
        gap: var(--spacing-lg);
    }

    .hero-title {
        font-size: 4rem;
        font-weight: 800;
        line-height: 1;
        color: hsl(var(--foreground));
        margin: 0;
    }

    .hero-description {
        font-size: 1.25rem;
        color: hsl(var(--muted-foreground));
        line-height: 1.6;
        margin: 0;
    }

    .hero-actions {
        display: flex;
        gap: var(--spacing-md);
    }

    .hero-button {
        padding: var(--spacing-sm) var(--spacing-xl);
        border-radius: var(--radius);
        border: none;
        font-weight: 600;
        cursor: pointer;
        transition: transform var(--transition-fast), background-color var(--transition-fast);
    }

    .hero-button:hover { transform: translateY(-2px); }

    .hero-button.primary {
        background-color: hsl(var(--primary));
        color: hsl(var(--primary-foreground));
    }

    .hero-button.secondary {
        background-color: hsl(var(--secondary));
        color: hsl(var(--secondary-foreground));
    }

    .hero-visual {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .hero-blob {
        width: 400px;
        height: 400px;
        background: linear-gradient(135deg, hsl(var(--primary)), hsl(var(--accent)));
        border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
        animation: morph 8s ease-in-out infinite;
    }

    @keyframes morph {
        0% { border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; }
        50% { border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%; }
        100% { border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%; }
    }

    @media (max-width: 768px) {
        .hero-container { grid-template-columns: 1fr; text-align: center; }
        .hero-title { font-size: 2.5rem; }
        .hero-actions { justify-content: center; }
        .hero-blob { width: 300px; height: 300px; }
    }
</style>
